file(GLOB SOURCE_FILES *.cpp)
file(GLOB INCLUDE_FILES *.h)

if (CMAKE_BUILD_TYPE STREQUAL "Debug")
    add_executable(hello_pipelined hello_pipeline.cpp)
    target_link_libraries(hello_pipelined PRIVATE ivfd ivfuid generatord gladd glfw imgui::imgui PNG::PNG JPEG::JPEG ZLIB::ZLIB glfw GLEW::GLEW OpenGL::GL)
    target_include_directories(hello_pipelined PRIVATE ${Stb_INCLUDE_DIR})
    add_dependencies(hello_pipelined gladd generatord ivfd ivfuid)
    set_target_properties(hello_pipelined PROPERTIES FOLDER "examples")
else()
    add_executable(hello_pipeline hello_pipeline.cpp)
    target_link_libraries(hello_pipeline PRIVATE ivf ivfui generator glad glfw imgui::imgui PNG::PNG JPEG::JPEG ZLIB::ZLIB glfw GLEW::GLEW OpenGL::GL)
    target_include_directories(hello_pipeline PRIVATE ${Stb_INCLUDE_DIR})
    add_dependencies(hello_pipeline glad generator ivf ivfui  )
    set_target_properties(hello_pipeline PROPERTIES FOLDER "examples")
endif() 
