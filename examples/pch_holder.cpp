// Empty translation unit whose sole purpose is to compile the shared examples PCH once.
// All example targets that have identical compile flags reuse the resulting .pch file
// via target_precompile_headers(example REUSE_FROM ivf2_examples_pch).
