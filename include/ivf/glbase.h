#pragma once

#include <ivf/base.h>

#include <glad/glad.h>

namespace ivf {

/**
 * Base class for all OpenGL objects in the ivf library.
 * 
 * This class is used to define a common interface for all OpenGL objects in the ivf library.
 * Currently it is an empty class, but it can be extended in the future to include common
 * functionality.
 */    
class GLBase : public Base {};

}; // namespace ivf
