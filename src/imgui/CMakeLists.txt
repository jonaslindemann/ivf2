INCLUDE_DIRECTORIES(${ivf2_SOURCE_DIR}/include)
FILE(GLOB SOURCE_FILES *.cpp)
FILE(GLOB INCLUDE_FILES ${ivf2_SOURCE_DIR}/include/imgui/*.h)

ADD_LIBRARY(imgui ${INCLUDE_FILES} ${SOURCE_FILES})
INSTALL_TARGETS(/lib imgui)

